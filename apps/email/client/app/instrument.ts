// Sentry was wired here in the SaaS build. Removed in Phase-1 - the
// self-hosted product brings its own observability stack if any. The
// file is kept so any existing entry-point import still resolves.
export {};
